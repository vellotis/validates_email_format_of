en:
  activemodel: &errors
    errors:
      messages:
        invalid_email_address: 'does not appear to be a valid e-mail address'
        email_address_not_routable: 'is not routable'
        email_address_not_pingable: 'is not pingable'
        mx_timeout: 'MX record processing timed out'
  activerecord:
    <<: *errors
